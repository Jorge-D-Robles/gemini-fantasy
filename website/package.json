{
  "name": "jrpg-tutorial-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for the JRPG in Godot 4 tutorial series",
  "scripts": {
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify && node build.js",
    "preview": "npx serve dist",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "highlight.js": "^11.11.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
