# gdlint configuration for Gemini Fantasy
# Matches project style: tabs, 100-char lines, double quotes
max-line-length: 100
max-file-lines: 1200
tab-characters: 4

# Disable rules that conflict with Godot conventions:
# function-name: we use _on_* callbacks and Godot virtuals
# class-name: autoloads don't use class_name
disable:
  - function-name
  - class-name
